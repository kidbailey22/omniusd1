import React from 'react'
import ReactDOM from 'react-dom/client'
import OmniUSD from './OmniUSD.jsx'

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <OmniUSD />
  </React.StrictMode>
)
